{
  "name": "@angular-helpers/core",
  "version": "21.14.0",
  "description": "Shared internal utilities and core logic for Angular Helpers packages",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Gaspar1992/angular-helpers"
  },
  "sideEffects": false,
  "scripts": {
    "build": "ng-packagr -p ng-package.json",
    "build:prod": "ng-packagr -p ng-package.json && find ../../dist/core -name '*.map' -delete && find ../../dist/core -name '*.mjs' -exec sed -i '/^\\/\\/#  sourceMappingURL=/d' {} +",
    "test": "vitest run",
    "lint": "oxlint src"
  },
  "peerDependencies": {
    "@angular/core": "^21.0.0"
  }
}
